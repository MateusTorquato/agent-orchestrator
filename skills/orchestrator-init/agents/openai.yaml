interface:
  display_name: "Orchestrator Init"
  short_description: "Detect and configure AI agent routes"
  brand_color: "#2563EB"
  default_prompt: "Use $orchestrator-init in Plan mode to detect AI routes and choose defaults with selectable questions."
policy:
  allow_implicit_invocation: true
