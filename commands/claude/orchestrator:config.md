---
description: View or edit Agent Orchestrator config.
---

# Agent Orchestrator Config

Use the `orchestrator-init` skill in config editing mode.

Read `~/.config/ai-orchestrator/config.yaml`, summarize the current settings, and apply requested changes only after showing the proposed diff or replacement.

## Arguments

`$ARGUMENTS`
