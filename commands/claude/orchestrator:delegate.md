---
description: Delegate one task to the best configured AI route.
---

# Agent Orchestrator Delegate

Use the `orchestrator-delegate` skill.

If `~/.config/ai-orchestrator/config.yaml` is missing, stop and route to `/orchestrator:init`.

## Arguments

`$ARGUMENTS`
