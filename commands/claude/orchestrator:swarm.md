---
description: Coordinate two or more distinct AI routes for swarm validation, competition, or worktree implementation.
---

# Agent Orchestrator Swarm

Use the `orchestrator-swarm` skill.

If `~/.config/ai-orchestrator/config.yaml` is missing, stop and route to `/orchestrator:init`.

Before dispatching, present a concise swarm plan and ask for confirmation.

## Arguments

`$ARGUMENTS`
