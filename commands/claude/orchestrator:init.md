---
description: Initialize Agent Orchestrator by detecting AI CLIs, local models, harness configs, and creating config after confirmation.
---

# Agent Orchestrator Init

Use the `orchestrator-init` skill.

Run passive detection first. It may write `~/.config/ai-orchestrator/inventory.json`.

Do not run smoke tests, write `config.yaml`, or install commands/plugins until the user confirms.

## Arguments

`$ARGUMENTS`
